class CanonBall {
  constructor(x, y) {
    var options = {
      "density" :   1.5,
      "friction" : 1.0,
      "restitution" : 0.8,
      "frictionAir":0.06
    }
  };

  display(){
    img2 = loadImage("image (11).png");
      image(img,600,200,2);

  };
};
